.vscode/**
.claude/**
.github/**
.bumpy/**
.yarn/**
.yarnrc.yml
.gitattributes
.gitignore
scripts/**
src/**
media/**
!media/jisho.svg
**/__tests__/**
**/*.spec.*
**/*.test.*
**/*.map
**/*.ts
!dist/**/*.d.ts
vite.config.*
tsconfig.json
yarn.lock
CLAUDE.md

# The dictionary DB is delivered by download on first activation, not bundled.
assets/**

# Exclude node_modules by default, but the Turso native runtime must ship in the .vsix
# (it is `neverBundle`d, so it is loaded from node_modules at runtime).
node_modules/**
!node_modules/@tursodatabase/**
